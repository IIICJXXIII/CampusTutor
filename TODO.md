# CampusTutor 项目完成度分析与待办清单

本计划对比 `mission.md` 中的需求规格，评估当前代码完成度，并列出改进建议。

## 一、核心业务主线完成度

| 业务节点 | 后端 | 前端 | 对接 | 状态 |
| :--- | :---: | :---: | :---: | :--- |
| **教师简化认证** | ✅ 认证提交/审核状态 | ✅ OCR 模拟页面 | ❌ | ⚠️ 缺 OCR 真实调用 |
| **家长发布需求** | ✅ CRUD + LBS 索引 | ✅ 三步表单 UI | ❌ | 需对接 |
| **AI 分层匹配** | ✅ 多维度搜索排序 | ✅ 教师列表展示 | ❌ | 需对接 |
| **试课签约** | ✅ 创建订单 API | ✅ 预约+签约流程 | ❌ | 需对接 |
| **课时记录** | ❌ 未实现打卡表 | ✅ 打卡 UI 完整 | ❌ | 缺后端 |
| **模拟支付结算** | ✅ 支付+托管+结算 | ✅ 2s 模拟支付 | ❌ | 需对接 |

## 二、后端功能完成评估 (对照 mission.md)

### ✅ 已完成 (MVP 核心)

| 功能模块 | 实现情况 |
| :--- | :--- |
| 用户注册/登录 (JWT) | 完整，支持密码+验证码 |
| 教员认证提交 | 完整，状态流转正确 |
| 教员档案管理 | 完整，含排课配置 |
| 家长学生管理 | 完整 CRUD |
| 需求发布与管理 | 完整，含上下架、LBS 索引 |
| 智能匹配搜索 | 完整，多维度筛选+排序 |
| 订单全流程 | 创建→支付→托管→完成/取消 |
| 钱包服务 | 余额+冻结/解冻 |
| 文件上传 | 本地存储 |

### ❌ 未实现 (mission.md 要求)

| 缺失功能 | mission.md 描述 | 优先级 |
| :--- | :--- | :---: |
| **百度 OCR 集成** | 调用百度 AI 识别学生证 | 🔴 高 |
| **课时打卡** | GPS 定位 + 拍照 + 家长确认 | 🔴 高 |
| **腾讯会议 API** | 试课自动生成会议链接 | 🟡 中 |
| **评价系统** | 课后星级评分 | 🟡 中 |
| **资金流水记录** | 交易明细追踪 | 🟡 中 |
| **提现功能** | 教员余额提现 | 🟡 中 |
| **错题本 OCR** | 错题拍照识别 | 🟢 低 |
| **IM 聊天** | 即时通讯 | 🟢 低 |

## 三、前端完成评估

### ✅ UI 层面 (基本完成)
- 15+ 页面已完成，风格统一
- 角色切换、表单流程、状态流转逻辑完整
- Tailwind CSS 响应式布局

### ❌ 对接层面 (完全缺失)

| 问题 | 现状 | 解决方案 |
| :--- | :--- | :--- |
| 无 API 封装 | axios 已安装但未使用 | 创建 `src/api/request.js` |
| 无 Vite 代理 | 跨域未处理 | 配置 `vite.config.js` proxy |
| 全部 Mock 数据 | 硬编码教师/需求列表 | 替换为 API 调用 |
| 登录硬编码 | 固定手机号切换角色 | 对接 `/api/auth/login` |

## 四、前后端连接评估

| 检查项 | 后端 | 前端 | 状态 |
| :--- | :---: | :---: | :---: |
| CORS 配置 | ✅ 已允许全部来源 | - | 通过 |
| API 基础路径 | `http://localhost:8080` | ❌ 未配置 | 阻断 |
| JWT 认证流程 | ✅ Bearer Token | ❌ 无 Token 管理 | 阻断 |
| 接口文档 | ✅ Knife4j 已启用 | ✅ API_REFERENCE.md | 就绪 |

**结论**：后端已就绪，前端需完成 API 层封装后方可连接。

## 五、优先改进清单

### ✅ Step 1: 前端 API 层搭建 (已完成)
1. ✅ 创建 `src/api/request.js` 封装 axios
2. ✅ 修改 `vite.config.js` 添加 `/api` 代理
3. ✅ 改造 `Login.vue` 对接 `/api/auth/login`

### ✅ Step 2: 后端补充课时打卡模块 (已完成)
1. ✅ 创建 `TeachingRecord` 实体对应 `teaching_record` 表
2. ✅ 实现打卡 API（开始/结束上课）
3. ✅ 实现家长确认/申诉 API

### ✅ Step 3: 集成百度 OCR (已完成)
1. ✅ 在后端添加 `OcrService` 调用百度 AI
2. ✅ 支持学生证/身份证识别
3. ✅ 添加模拟模式便于开发测试

### ✅ Step 4: 逐步替换前端 Mock 数据 (已完成)
1. ✅ `OrderList.vue` → `/api/order/parent` 或 `/tutor`
2. ✅ `TeacherList.vue` → `/api/match/tutors`
3. ✅ `TeacherAuth.vue` → OCR + 认证提交
4. ✅ `DemandForm.vue` → `/api/demand`
5. ✅ `Booking.vue` + `Payment.vue` → `/api/order`

### Further Considerations
- **密码加密**：后端目前使用 MD5，建议升级为 BCrypt
- **权限控制**：缺少基于角色的 API 访问控制（家长不能调用教员接口）
- **演示数据**：`mission.md` 要求预置 10 个教师 + 8 个需求，需准备 SQL 脚本
